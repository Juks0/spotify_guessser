{
  "name": "spotify-guesser-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "build": "npm run build -w frontend && npm run build -w backend",
    "build:frontend": "npm run build:prod -w frontend",
    "build:backend": "npm run build -w backend",
    "start": "concurrently \"npm run start -w frontend\" \"npm run start -w backend\"",
    "start:frontend": "npm run start -w frontend",
    "start:backend": "npm run start -w backend",
    "install:all": "npm install && npm install -w frontend && npm install -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.0.0"
  }
}

